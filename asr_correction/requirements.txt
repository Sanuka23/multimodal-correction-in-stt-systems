# ASR Correction Module Dependencies
#
# Core text processing: no external dependencies (pure Python)
#
# ML backend — install ONE of the following:

# Option 1: MLX (Apple Silicon — M1/M2/M3/M4)
mlx-lm>=0.12.0

# Option 2: Transformers (Linux / CUDA)
# transformers>=4.40.0
# peft>=0.10.0
# torch>=2.2.0
# accelerate>=0.28.0

# Evaluation Dashboard (optional)
# pip install streamlit plotly pandas python-dotenv boto3
streamlit>=1.30.0
plotly>=5.18.0
pandas>=2.0.0
python-dotenv>=1.0.0
boto3>=1.34.0  # For S3 live data loading
