# FastAPI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Auth
python-jose[cryptography]>=3.3.0

# MongoDB
motor>=3.3.0

# Evaluation
jiwer>=3.0.0

# Templates
jinja2>=3.1.0

# ML Backend (Apple Silicon)
mlx>=0.5.0; sys_platform == "darwin" and platform_machine == "arm64"
mlx-lm>=0.12.0; sys_platform == "darwin" and platform_machine == "arm64"

# ML Backend (Linux/CUDA) - uncomment if needed
# transformers>=4.40.0
# peft>=0.10.0
# torch>=2.2.0

# Video OCR extraction
paddlepaddle>=2.6.0
paddleocr>=2.9.0
opencv-python-headless>=4.9.0

# Whisper Pass 2 (re-transcribe flagged segments with vocab hints)
faster-whisper>=1.0.0

# AVSR (lip-reading hints — MediaPipe mode)
mediapipe>=0.10.0

# AVSR (full lip-reading — Auto-AVSR mode, optional)
# Install via: bash scripts/setup_auto_avsr.sh
# pytorch-lightning>=2.0.0
# sentencepiece>=0.1.99
# av>=10.0.0

# Training data pipeline
requests>=2.31.0
tqdm>=4.66.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6
